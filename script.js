const header=document.querySelector('header');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>20));const menu=document.querySelector('.menu'),links=document.querySelector('.links');if(menu)menu.onclick=()=>links.classList.toggle('open');document.querySelectorAll('.links a').forEach(a=>a.onclick=()=>links.classList.remove('open'));document.querySelectorAll('.smart-link').forEach(a=>{a.target='_blank';a.addEventListener('click',()=>a.href=`https://wa.me/573175930860?text=${encodeURIComponent(a.dataset.message)}`)});const els=document.querySelectorAll('.reveal');const ob=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');ob.unobserve(e.target)}}),{threshold:.12});els.forEach(e=>ob.observe(e));document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');const filter=b.dataset.filter;document.querySelectorAll('.item').forEach(i=>i.classList.toggle('hide',filter!=='all'&&i.dataset.cat!==filter));document.querySelectorAll('[data-heading]').forEach(h=>h.classList.toggle('hide',filter!=='all'&&h.dataset.heading!==filter))});const modal=document.querySelector('#modal');if(modal){const box=modal.querySelector('div');document.querySelectorAll('.item').forEach(i=>{i.onmouseenter=()=>i.querySelector('video')?.play();i.onmouseleave=()=>i.querySelector('video')?.pause();i.onclick=()=>{box.innerHTML=i.dataset.video?`<video controls autoplay muted><source src="${i.dataset.video}"></video>`:`<img src="${i.dataset.img}" alt="Vista ampliada">`;modal.showModal()}});modal.querySelector('.close').onclick=()=>modal.close();modal.onclick=e=>{if(e.target===modal)modal.close()}}const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();

// V11: muestra errores devueltos por el formulario sin cambiar el diseño.
const params=new URLSearchParams(location.search);if(params.has('error')){const form=document.querySelector('.oasis-form');if(form){const p=document.createElement('p');p.className='error-banner';const code=params.get('error');p.textContent=code==='2'?'No fue posible enviar el mensaje. Escríbenos por WhatsApp mientras revisamos el servicio de correo.':code==='3'?'El formulario aún no tiene configurado el servicio de correo en Vercel. Escríbenos por WhatsApp.':'Revisa los campos obligatorios e inténtalo nuevamente.';form.prepend(p)}}
